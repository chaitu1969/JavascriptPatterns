// logger.js
const Logger = {
  log: (message) => console.log(message),
  error: (message) => console.error(message),
};

module.exports = Logger;

/**
 *
 * With Module split code into smaller pieces, with each functionality
 * imporces readablity and testing
 *
 */
